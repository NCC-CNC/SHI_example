/**
 * App shell for M0. This is intentionally a placeholder: it confirms the
 * scaffold builds, deploys, and renders. The grid, time slider, and score
 * panels arrive in later milestones (see docs/design/03_milestones.md).
 */
export function App() {
  return (
    <main className="app">
      <h1>Species Habitat Index Explorer</h1>
      <p>
        An interactive tool for exploring the concepts behind the Species Habitat Index
        (SHI). This is an early scaffold; the interactive grid, time slider, and index
        readouts are coming next.
      </p>
      <p className="note">See the project design docs for what is being built.</p>
    </main>
  );
}
