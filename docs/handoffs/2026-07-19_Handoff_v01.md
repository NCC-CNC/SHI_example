# Session Handoff: 2026-07-19

## Summary

Short session. Added a self-contained "solo build-effort estimate" page,
modeled on the equivalent page in the sibling SCP_tutorial project. One PR
merged; issue #54 closed. No app behaviour or engine changes.

Live app: https://ncc-cnc.github.io/SHI_example/
Effort page: https://ncc-cnc.github.io/SHI_example/effort-estimate.html
Repo: https://github.com/NCC-CNC/SHI_example

## Completed

Build-effort estimate page (PR #55, closed #54)

- New public/effort-estimate.html: self-contained (inline CSS + JS, no
  dependencies), theme-aware (light/dark), responsive. Vite copies public/
  verbatim into the Pages build, so it deploys at /effort-estimate.html.
- Estimates the solo effort to build the app at the same quality bar without
  heavy AI assistance, split across three personas by starting knowledge:
  P1 knows both (14-20d), P2 stack only (19-27d), P3 SHI only (40-75+d,
  open-ended).
- Scope strip uses the real repo metrics at main @ d1e10bb: 2,495 src +
  1,198 test LOC, 90 tests (~99% engine coverage), 33 merged PRs, 7 design
  notes/ADRs, ~2 days AI-assisted actual.
- Scaled down from the SCP_tutorial page: no ILP solver, no wasm/Web Worker,
  SVG not canvas, so the hardest slice here is a faithful SHI model plus the
  before/after edit loop rather than solver integration. Added a synthetic-data
  caveat the SCP page does not have.

## Current state

What works:

- Everything from the 2026-07-17 handoff, unchanged. Full pipeline live on the
  generated ~30x30 landscape, layered documentation (guided tour, About modal,
  inline tooltips), accessible edit grid, change-impact under the map.
- Plus the new effort-estimate page reachable at /effort-estimate.html.
- 90 tests pass; CI green; deploy automated.

Notes:

- The persona day-ranges and the ~2-day AI actual are estimates, not measured.
  Owner confirmed the ranges are good this session. Tuning any bar is a one-line
  edit in public/effort-estimate.html.
- Verified in a real browser (Playwright + Chromium) in both light and dark
  schemes before shipping. format:check clean, build copies the file into dist/.

## Next actions

Backlog unchanged from 2026-07-17. Each carries its own design cost:

1. #18 Ingest real ESA CCI land cover: largest open design question, needs an
   ADR first, gates #19 and #20. Recommended next if picking up feature work.
2. #39 Scale species toward ~100.
3. #19 Elevation layer and per-species bands (gated by #18).
4. #20 Population-weighted Steward's SHI (gated by #18).
5. #21 Edit propagation forward through the time series.

## Risks or decisions needed

- None new this session.
- Standing: #18 (real data) needs an ADR before building; it is the largest
  remaining design decision and gates the elevation and Steward's SHI work.
